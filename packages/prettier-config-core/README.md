# @operationmonkey/prettier-config-core

## Install

```
npm install --save-exact --save-dev @operationmonkey/prettier-config-core
```

## Use

Create `prettier.config.js` containing:

```
module.exports = require("@operationmonkey/prettier-config-core")
```
